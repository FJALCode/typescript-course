(function () {
    const nombre = 'Fernando';
    const apellido = 'Antúnez';
    const edad = 28;

    // const salida = nombre + ' ' + apellido + ' ( ' + edad + ' )';

    const salida = `${nombre} ${apellido} ( ${edad} )`;

    console.log(salida);       

})();